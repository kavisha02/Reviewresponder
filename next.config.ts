import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No special config needed for Phase 1
  // We'll add Google OAuth domains and API routes in later phases
};

export default nextConfig;
